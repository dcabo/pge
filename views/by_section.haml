%h1 Expenses by section

%table#pge.display
	%thead
		%tr
			%th Section
			%th Amount ('000)
	%tbody
		-@sections.each do |s|
			%tr
				%td
					%a{:href=>"/section/#{s.section}"} #{s.description}
				%td.right #{s.amount.to_i} €
